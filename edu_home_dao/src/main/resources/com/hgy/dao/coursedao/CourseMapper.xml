<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hgy.dao.coursedao.CourseMapper">
    <!--多条件查询课程信息-->
    <select id="findCourseByCondition" parameterType="com.hgy.gobal.CourseVO" resultType="course">
        SELECT * FROM course
        <where>
            <if test="courseName != null and courseName != ''">
                AND course_name LIKE CONCAT('%',#{courseName},'%')
            </if>

            <if test="status != null">
                AND status = #{status}
            </if>

            <if test="true">
                AND is_del = 0
            </if>
        </where>
    </select>

    <!--新增课程信息-->
    <insert id="addCourse" parameterType="course">
        INSERT INTO course(
            course_name,
            brief,
            preview_first_field,
            preview_second_field,
            course_img_url,
            course_list_img,
            sort_num,
            price,
            discounts,
            sales,
            discounts_tag,
            course_description_mark_down,
            create_time,
            update_time
        ) VALUES(#{courseName},#{brief},#{previewFirstField},#{previewSecondField},#{courseImgUrl},
                 #{courseListImg},#{sortNum},#{price},#{discounts},#{sales},#{discountsTag},#{courseDescriptionMarkDown},
                 #{createTime},#{updateTime});

        <!--插入插座结束后获取插入记录的id值-->
        <selectKey resultType="int" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <!--新增讲师信息-->
    <insert id="addTeacher" parameterType="teacher">
        INSERT INTO teacher(
            course_id,
            teacher_name,
            position,
            description,
            create_time,
            update_time
        ) VALUES(#{courseId},#{teacherName},#{position},#{description},#{createTime},#{updateTime});
    </insert>

    <!--根据id查询课程及对应讲师信息-->
    <select id="findCourseById" parameterType="int" resultType="com.hgy.gobal.CourseVO">
        SELECT c.*, t.teacher_name, t.position, t.description FROM course c
            LEFT JOIN teacher t ON c.id = t.course_id WHERE c.id = #{id}
    </select>

    <!--更新课程信息-->
    <update id="updateCourse" parameterType="course">
        UPDATE course
        <trim prefix="SET" suffixOverrides=",">
            <if test="courseName != null and courseName != ''">
                course_name = #{courseName},
            </if>

            <if test="brief != null and brief != ''">
                brief = #{brief},
            </if>

            <if test="previewFirstField != null and previewFirstField != ''">
                preview_first_field = #{previewFirstField},
            </if>

            <if test="previewSecondField != null and previewSecondField != ''">
                preview_second_field = #{previewSecondField},
            </if>

            <if test="courseImgUrl != null and courseImgUrl != ''">
                course_img_url = #{courseImgUrl},
            </if>

            <if test="courseListImg != null and courseListImg != ''">
                course_list_img = #{courseListImg},
            </if>

            <if test="sortNum != null and sortNum != ''">
                sort_num = #{sortNum},
            </if>

            <if test="price != null and price != ''">
                price = #{price},
            </if>

            <if test="discounts != null and discounts != ''">
                discounts = #{discounts},
            </if>

            <if test="sales != null and sales != '' or sales==0">
                sales = #{sales},
            </if>

            <if test="discountsTag != null and discountsTag != ''">
                discounts_tag = #{discountsTag},
            </if>

            <if test="courseDescriptionMarkDown != null and courseDescriptionMarkDown != ''">
                course_description_mark_down = #{courseDescriptionMarkDown},
            </if>

            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </trim>
        WHERE id = #{id}
    </update>

    <!--更新讲师信息-->
    <update id="updateTeacher" parameterType="teacher">
        UPDATE teacher
        <trim prefix="SET" suffixOverrides=",">
            <if test="teacherName != null and teacherName != ''">
                teacher_name = #{teacherName},
            </if>

            <if test="position != null and position != ''">
                position = #{position},
            </if>

            <if test="description != null and description != ''">
                description = #{description},
            </if>

            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </trim>
        WHERE course_id = #{courseId}
    </update>

    <!--课程状态管理-->
    <update id="updateCourseStatus" parameterType="course">
        UPDATE course SET status = #{status} WHERE id = #{id}
    </update>
</mapper>